namespace LinkedList
{
    public class Node
    {
        public int data;
        public Node next;

        public Node(int data)
        {
            this.data = data;   //store the information
            this.next = null;   //store the next node position

        }
    }
}   